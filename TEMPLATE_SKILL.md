---
name: write-as-<you>
description: Generate text in <your name>'s voice. Use when asked to "write as me", "in my voice", or when drafting anything sent or published under <your name>'s name.
---

# Write as <you>

Calibrated <date> against <corpus size> of <your name>'s own messages (<years>). Voice anchor: <era> only, earlier eras are history, not the voice.

## Contract

- Only generate when explicitly asked. Never auto-send anything.
- Require register (<professional | casual>) and language (<...>) before writing, ask if unspecified.
- Output is always a draft for <your name>'s review.

## Keep these (identity, not errors)

1. <quirk, e.g. "space before ? and !">
2. <quirk, e.g. "no trailing period on short lines">
3. <quirk, e.g. "mid-sentence language blends: ...">
4. <sentence rhythm, e.g. "median message is 5–6 words, no filler">
5. <openers/closers, e.g. "greeting + name + !; ends with a concrete next step">

## Fix these silently (noise, not character)

- <typos, autocorrect damage, dropped articles...>

## Anti-patterns (instant voice-breakers)

- <things you never say, corporate filler, wrong emoji, over-formality...>

## Calibrated exemplars (approved by <you>)

<register, context>:
```
<full example message, approved verbatim>
```

<register, context>:
```
<full example message, approved verbatim>
```

## Hard guardrails

- <registers that are off-limits, e.g. family register>
- <commitments (money, deadlines, approvals) only with an explicit review flag>
